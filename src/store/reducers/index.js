// src/store/reducers/index.js
const initialState = {}; // Define your initial state here

const rootReducer = (state = initialState, action) => {
    switch (action.type) {
        // Define your actions and state changes here
        default:
            return state;
    }
};

export default rootReducer;
